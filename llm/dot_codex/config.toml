# Codex CLI Configuration
# This file is symlinked to ~/.codex/config.toml

# Model selection
model = "gpt-5.4"
model_reasoning_effort = "medium"

# Default Main agent behavior
developer_instructions = '''
You are the default Main agent and should behave like a senior project planner/orchestrator.

- For non-trivial tasks, plan first: clarify the goal, constraints, risks, verification path, and the smallest useful route before acting.
- For trivial, low-risk informational tasks, answer or inspect/read directly without unnecessary delegation.
- Use adaptive routing. Do not invoke all agents by default; delegate only when a subagent reduces concrete uncertainty, risk, implementation cost, or verification cost.
- Keep custom agents as subagents. Route external documentation/web/API research to searcher; implementation to coder when delegation is useful; code review to reviewer; adversarial failure/regression design to cracker; command-only execution to runner; human-facing prose to writer. Wait for required subagent results and integrate them into the final work.
- Every subagent dispatch must include goal, assigned scope, role-relevant context only, acceptance criteria, stop condition, and expected output. Do not pass full conversation history by default.
- Prefer the smallest useful subagent set, usually zero or one agent for simple work and one targeted reviewer pass after meaningful code changes unless extra review materially reduces risk.
- If the same failure repeats twice, stop retrying, report the failure pattern, and revise the plan or route differently.
- You may inspect/read, plan, coordinate, answer, run non-mutating verification, and dispatch subagents directly when that is the simplest appropriate route; do not treat planning as a prohibition on useful orchestration work.
- Main/default must not directly create, modify, delete, rename, or overwrite files, and must not use file-mutating tools or commands directly for project changes. Delegate file changes to coder or another appropriate subagent with clear scope and acceptance criteria.
'''

# Permissions
sandbox_mode = "workspace-write"
approval_policy = "never"

# MCP Servers
notify = ["/Users/geometriccross/.config/dotfiles/llm/dot_codex/computer-use/Codex Computer Use.app/Contents/SharedSupport/SkyComputerUseClient.app/Contents/MacOS/SkyComputerUseClient", "turn-ended"]

[mcp_servers.fetch]
command = "docker"
args = ["run", "-i", "--rm", "mcp/fetch"]

[mcp_servers.context7]
url = "https://mcp.context7.com/mcp"

[mcp_servers.playwright]
enabled = true
command = "/usr/bin/env"
args = ["PATH=/Users/geometriccross/.local/share/aquaproj-aqua/pkgs/http/nodejs.org/dist/v25.9.0/node-v25.9.0-darwin-arm64.tar.gz/node-v25.9.0-darwin-arm64/bin:/usr/bin:/bin", "/Users/geometriccross/.local/share/aquaproj-aqua/pkgs/http/nodejs.org/dist/v25.9.0/node-v25.9.0-darwin-arm64.tar.gz/node-v25.9.0-darwin-arm64/bin/node", "/Users/geometriccross/.local/share/aquaproj-aqua/pkgs/http/nodejs.org/dist/v25.9.0/node-v25.9.0-darwin-arm64.tar.gz/node-v25.9.0-darwin-arm64/lib/node_modules/npm/bin/npx-cli.js", "-y", "@playwright/mcp@0.0.73", "--headless"]
startup_timeout_sec = 30
tool_timeout_sec = 120

[mcp_servers.node_repl]
args = []
command = "/Applications/Codex.app/Contents/Resources/cua_node/bin/node_repl"
startup_timeout_sec = 120

[mcp_servers.node_repl.env]
NODE_REPL_NATIVE_PIPE_CONNECT_TIMEOUT_MS = "1000"
NODE_REPL_NODE_MODULE_DIRS = "/Applications/Codex.app/Contents/Resources/cua_node/lib/node_modules"
NODE_REPL_NODE_PATH = "/Applications/Codex.app/Contents/Resources/cua_node/bin/node"
NODE_REPL_TRUSTED_CODE_PATHS = "/Users/geometriccross/.codex"
CODEX_HOME = "/Users/geometriccross/.codex"
NODE_REPL_TRUSTED_BROWSER_CLIENT_SHA256S = "89809670b247cbaffbc4f87d9fa9742a9038c0f4a06fb822d7f85ff19bf24215"
BROWSER_USE_AVAILABLE_BACKENDS = "chrome,iab"
NODE_REPL_INSTRUCTIONS_USE_CASE_BROWSER = "Control the in-app browser in conjunction with the Browser Plugin."
NODE_REPL_INSTRUCTIONS_USE_CASE_CHROME = "Control the Chrome browser in conjunction with the Chrome Plugin. Prefer this method of controlling Chrome over alternatives (such as Computer Use) unless the user explicitly mentions an alternative."
BROWSER_USE_CODEX_APP_BUILD_FLAVOR = "prod"
BROWSER_USE_CODEX_APP_VERSION = "26.609.71450"
CODEX_CLI_PATH = "/Applications/Codex.app/Contents/Resources/codex"

[projects."/Users/geometriccross"]
trust_level = "trusted"

[projects."/Users/geometriccross/.config/dotfiles"]
trust_level = "trusted"

[projects."/Users/geometriccross/Temp/qiime"]
trust_level = "trusted"

[projects."/Users/geometriccross/gdrive/研究/発表/日本獣医学術集会"]
trust_level = "trusted"

[projects."/Users/geometriccross/ghq/github.com/geometriccross/q2lsp"]
trust_level = "trusted"

[tui.model_availability_nux]
"gpt-5.5" = 4

[notice.model_migrations]
"gpt-5.3-codex" = "gpt-5.4-mini-fast"
"gpt-5.4" = "gpt-5.4-mini-fast"

[plugins."github@openai-curated"]
enabled = true

[plugins."documents@openai-primary-runtime"]
enabled = true

[plugins."spreadsheets@openai-primary-runtime"]
enabled = true

[plugins."presentations@openai-primary-runtime"]
enabled = true

[plugins."browser@openai-bundled"]
enabled = true

[plugins."pdf@openai-primary-runtime"]
enabled = true

[marketplaces.openai-bundled]
last_updated = "2026-06-17T01:40:55Z"
source_type = "local"
source = "/Users/geometriccross/.config/dotfiles/llm/dot_codex/.tmp/bundled-marketplaces/openai-bundled"

[marketplaces.openai-primary-runtime]
last_updated = "2026-06-15T16:12:59Z"
source_type = "local"
source = "/Users/geometriccross/.cache/codex-runtimes/codex-primary-runtime/plugins/openai-primary-runtime"

[features]
js_repl = false

[desktop.open-in-target-preferences]
global = "ghostty"

[desktop.open-in-target-preferences.perPath]
"/Users/geometriccross/ghq/github.com/geometriccross/q2lsp" = "ghostty"
